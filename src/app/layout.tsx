import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"], variable: "--font-inter" });

export const metadata: Metadata = {
  title: "InsightFlow AI — Research → Evidence → Decisions",
  description: "Transform messy research into trustworthy business decisions in minutes.",
  keywords: ["AI research", "business intelligence", "evidence-based decisions", "RAG", "knowledge base"],
};

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`} suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}
